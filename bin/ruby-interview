#!/usr/bin/env ruby
# frozen_string_literal: true

require_relative '../lib/data_processor'

closest_shops = DataProcessor.new(*ARGV).get_closest_shops(3)

puts "These are the #{closest_shops.count} closest coffee shops we found:"
closest_shops.each do |shop|
  puts "#{shop[:name]} is #{shop[:distance]} km away"
end
